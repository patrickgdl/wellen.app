export interface CanvasAudioStyle {
    style: string; // cfg.style || 'lounge';
    barWidth: number; // cfg.barWidth || 2;
    barHeight: number; // cfg.barHeight || 2;
    barSpacing: number; // cfg.barSpacing || 5;
    barColor: string; // cfg.barColor || '#ffffff';
    shadowBlur: number; // cfg.shadowBlur || 10;
    shadowColor: string; // cfg.shadowColor || '#ffffff';
    font: string[]; // cfg.font || ['12px', 'Helvetica'];
}
